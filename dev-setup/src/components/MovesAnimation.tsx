import React from 'react'

type Props = { workoutId: string }

// Removed DOM output for demo animations. Keep component as a noop
// so imports remain valid when the animation is not desired.
export default function MovesAnimation(_: Props) {
  return null
}
