import React from 'react'

// Removed preview UI because there are no video assets.
// Keep the component so callers don't need edits; render nothing.
export default function StepMedia() {
  return null
}
